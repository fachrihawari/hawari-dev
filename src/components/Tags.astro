---
interface Props {
  tags: string[]
  classes?: string
}

const { tags, classes } = Astro.props
---

<div class={`flex flex-row gap-2 flex-wrap ${classes}`}>
  {
    tags.map((tag) => (
      <span class="px-2 rounded-md bg-gray-600 text-gray-50">
        #{tag}
      </span>
    ))
  }
</div>
